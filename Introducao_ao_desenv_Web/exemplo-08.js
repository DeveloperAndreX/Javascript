/** ParentNodes serve para retornar o elemento PAI*/
/** Se eu quiser retornar o elemento pai dessa div id="ilheus": document.getElementById("ilheus").parentNodes */

console.log(
    document.getElementById("ilheus").parentNode,
    // Ele vai retornar o elemento Pai;
    document.getElementById("brasil").parentNode.parentNode  // 2 vezes e atualizar a pagina;
    //Ele vai retornar "Brasil";
    // E dessa forma que funciona o Parent Node;

);

